# Core
anthropic>=0.18.0
openai>=1.12.0
gradio>=4.0.0
python-dotenv>=1.0.0

# TTS
google-cloud-texttospeech>=2.14.0
elevenlabs>=1.0.0
pydub>=0.25.1

# Image Generation
Pillow>=10.0.0
requests>=2.31.0
fal-client>=0.4.0          # fal.ai (기본 이미지 엔진)
google-generativeai>=0.3.0  # Google Imagen

# Audio/Video
# ffmpeg (system install required)
# ffprobe (included with ffmpeg)

# Whisper (optional - for accurate timestamps)
openai-whisper>=20231117

# YouTube (trend analysis, transcript extraction & upload)
google-api-python-client>=2.100.0
google-auth-oauthlib>=1.0.0      # OAuth2 인증 (YouTube 업로드용)
youtube-transcript-api>=0.6.1
